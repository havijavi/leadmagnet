# Drop your Google service-account JSON here as `google.json` and set
# GOOGLE_SHEETS_CREDENTIALS_FILE=/app/secrets/google.json in .env.
# This folder is mounted read-only into the backend container.
