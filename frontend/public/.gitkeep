# Next.js looks here for static assets (favicon.ico, robots.txt, images, etc.).
# Keep the folder so the Dockerfile's COPY --from=build /app/public step succeeds
# even before we drop any real assets in.
